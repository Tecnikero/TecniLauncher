<Window x:Class="TecniLauncher.VentanaMensaje"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Aviso" Height="200" Width="450"
        WindowStyle="None" ResizeMode="NoResize"
        AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" Topmost="True">

    <Border CornerRadius="15" Background="#1F1F1F" BorderBrush="#33FFFFFF" BorderThickness="1">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock x:Name="txtTitulo" Text="CONFIRMACIÓN" Foreground="White" FontWeight="Bold" FontSize="16"/>

            <TextBlock x:Name="txtMensaje" Grid.Row="1" Text="¿Estás seguro?" 
                       Foreground="#CCC" TextWrapping="Wrap" VerticalAlignment="Center" FontSize="14" Margin="0,10"/>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">

                <Button x:Name="btnCancelar" Content="CANCELAR" Height="35" Width="100" Margin="0,0,10,0"
                        Background="#333" Foreground="White" Visibility="Collapsed"
                        BorderThickness="0" FontWeight="Bold" Cursor="Hand"
                        Click="BtnCancelar_Click">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                </Button>

                <Button x:Name="btnAceptar" Content="ACEPTAR" Height="35" Width="100" 
                        Background="#5B4B8A" Foreground="White"
                        BorderThickness="0" FontWeight="Bold" Cursor="Hand"
                        Click="BtnAceptar_Click">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>